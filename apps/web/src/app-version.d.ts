/* Baked in by apps/web/vite.config.ts, and by vitest.config.ts for the suite,
   from scripts/app-version.ts. The version this bundle was BUILT as, which is
   not the version the server is running the moment a tab is left open across a
   deploy. */
declare const __APP_VERSION__: string
